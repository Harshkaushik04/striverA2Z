#include<bits/stdc++.h>
using namespace std;

void rotate(vector<int> nums,int k) { //right rotation
    int numsSize=nums.size();
    int actual_k=k%numsSize;
    int temp[actual_k],temp1[actual_k];
    
}